Understood on the demo trading limitation — that's a confirmed, documented platform restriction, not something to keep fighting. Switch to /mode -> simulated, but before I trust its output, confirm the following:

Show me exactly what market data source simulated mode currently uses. Is it MarketDataFeed/watch_order_book_loop pulling real live public order book data via ccxt.pro with no API key, or is it still using mock_orderbook_websocket_feed() with synthetic/fabricated data? I need this to be real live prices — that's the entire point of this test.
If it's still using the mock feed, wire it to the real public feed now, per the earlier fix: no API keys needed, since order book data is public. Confirm no credentials are required anywhere in this code path.
Once wired to real data, run it continuously and let me query /pnl and /opportunities to see genuine simulated results against real market conditions — not a one-off backtest, an ongoing live-data paper-trading run per spec Section 2.5 and the Go-Live Gate's evaluation window (Section 9).
Separately: keep the testnet path (Bybit/Binance testnet, per your Path 2) available as a lower-priority secondary check — that one's useful later for verifying order-placement mechanics work end-to-end, but it answers a different question than profitability and I don't need it right now.