Before anything else, fix item #5: implement the startup crash-recovery check per spec Section 2.3. Right now run_bot() doesn't query get_active_executions() on boot, which means any execution left in a non-terminal state (from a crash OR a mode-switch restart) is never picked up, re-verified against the exchange, or resolved into UNWOUND/STUCK before normal trading resumes. This is more serious than a standalone gap — the entire mode-switching design assumes restart = automatic recovery coverage, so until this is fixed, every demo↔testnet↔live switch has zero protection for trades that were mid-execution during the restart.

Implement this now: on startup, before the bot resumes opportunity scanning, query for all executions not in a terminal state (COMPLETED, FAILED, UNWOUND), and for each one, re-fetch actual order/balance status from the relevant exchange and resolve it into the correct terminal state or STUCK with an alert, per spec Section 2.3. Log a recovery report on every startup showing what was found, even if nothing was in-flight.

Once that's fixed, also clarify and show me code for the following:

In the fee calculator, you described the book walk as "naive." Clarify exactly what that means, and show me the full walk_order_book function. I want to confirm it's actually walking real depth correctly and not cutting a corner the word "naive" might be hinting at.
If I set BOT_MODE=live directly as an environment variable and start the process without ever going through the /switch_live Telegram command, does the bot refuse to start, or does it start and only block individual trades inside the loop? Show me the actual startup sequence in main.py for the live branch, and confirm whether the Go-Live Gate is checked before any live client is initialized, not just before individual trades execute.
For triangular arbitrage's partial-fill resizing, you only showed the branch for if prev_leg['side'] == 'buy'. Show me the corresponding sell-side branch. Also confirm explicitly: is the partial_fill_min_viable_pct threshold from the config actually wired in anywhere, so that a leg filled below that percentage triggers an unwind instead of a resize-and-continue? Show me that specific check, or confirm it's missing.
For funding rate arbitrage, confirm whether margin ratio / liquidation risk is wired in as a hard override on the exit logic, independent of and overriding the funding-trend and basis-risk exit conditions, per spec Section 17 (liquidation risk should force a close regardless of what the funding outlook says). Show me where in evaluate_exit() (or elsewhere) this check happens, or confirm it doesn't exist yet.

Give me the same honest format as before — for each of these four items plus the recovery-flow fix, tell me exactly what you changed or found, with file/function references. Don't round anything up to "done" if it's partial.